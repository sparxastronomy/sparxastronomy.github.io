---
import Timeline from "../../widgets/timeline.astro";
import projects from "../../../config/projects.json";
---


<div class="mx-auto w-4/5 md:w-auto md:max-w-[60%] justify-center items-center">
    <p class="text-center heading-3 md:heading-2 font-emphasis text-trust-600 dark:text-trust-500 my-2 mb-4">
        Research Timeline
    </p>
    <!-- Mapping the projects -->
    {
        projects.projects.map((item) =>(
            <Timeline item={item}/>
        ))
    }
</div>